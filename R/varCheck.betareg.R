varCheck.betareg <- function(model, xlab, ylab, pch, col, lcol, ...) {
  stop('varCheck is not available for betareg package yet, run ', 
       sQuote("funComp()"), ' to check which function you can use')
}
